import 'package:flutter/material.dart';

class TournametScreen extends StatelessWidget {
  const TournametScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
